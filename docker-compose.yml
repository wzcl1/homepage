services:
  homepage:
    build: .
    image: homepage:latest
    container_name: homepage
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DATA_PATH=/app/data/data.json
    volumes:
      - ./data:/app/data
    user: "1000:1000"